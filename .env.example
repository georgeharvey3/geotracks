# Copy this file to `.env` and fill in the values. Only variables prefixed
# with VITE_ are exposed to the client bundle.

# Base URL of the Firebase Realtime Database backing the leaderboard.
# The app reads/writes scores at `${VITE_FIREBASE_DB_URL}/scores.json`.
VITE_FIREBASE_DB_URL=https://geotracks-d9b5c-default-rtdb.europe-west1.firebasedatabase.app
